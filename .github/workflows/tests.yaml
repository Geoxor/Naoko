name: Node.js CI

on: [push]

jobs:
  test-sakuria:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: npm run setup
      - name: Compile
        run: npm run tests